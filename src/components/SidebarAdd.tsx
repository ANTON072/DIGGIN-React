import * as React from "react"
import styled from "styled-components"

const SidebarAdd: React.SFC<{}> = () => {
  return <Root>Add</Root>
}

export default SidebarAdd

const Root = styled.div`
  background-color: #fff;
  height: 261px;
  display: flex;
  justify-content: center;
  align-items: center;
`
