import * as React from "react"
import styled from "styled-components"

const SidebarLogo: React.SFC<{}> = () => {
  return <Root>Logo</Root>
}

export default SidebarLogo

const Root = styled.div`
  background-color: #fff;
  height: 291px;
  display: flex;
  justify-content: center;
  align-items: center;
`
